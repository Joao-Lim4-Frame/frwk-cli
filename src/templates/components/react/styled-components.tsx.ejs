//@Framework dev kit

import styled from 'styled-components';

/*
 * @name Container<%= props.name %>
 * @description 
 *   Emcapsula todo o componente para ter uma maior manipulação das suas classes
 *   e aumentar sua produtividade.
 */

const Container<%= props.name %> = styled.div`
    width: 100%;
    position: relative;
    text-align: center;
`;



export { 
    Container<%= props.name %> 
}